[audio]
; Loopback test only: back the modulator level DOWN to stay out of the
; null-sink path's saturation region so the modem sees a high-SNR channel.
; Not a real-radio setting.
tx_gain_db = -12.0
